apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: nodered
  name: nodered-data
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: storage-account
  resources:
    requests:
      storage: 5Gi
